create table customers1 (
	id serial primary key,
	name varchar,
	email varchar,
	active bool
)

create table customers2 (
	id serial primary key,
	name varchar,
	email varchar,
	active bool
)

create table customers3 (
	id serial primary key,
	name varchar,
	email varchar,
	active bool
)